services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - MAINTENANCE_MODE=false
    restart: unless-stopped
